--- a/configure
+++ b/configure
@@ -204,6 +204,10 @@ rm -f config.log
 run() {
   (
     printf '%s\n' "$*"
+    [ -n "$(echo "$1" | grep noop)" ] && exit 0
+    [ -n "$(echo "$1" | grep lm)" ] && exit 0
+    [ -n "$(echo "$1" | grep stdatomic)" ] && exit 0
+    [ -n "$(echo "$1" | grep zlib)" ] && exit 0
     if nice "$@"; then
       exit 0
     else
