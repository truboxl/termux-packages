#!/bin/sh

case $# in
	0) echo 'usage: ldd FILE...';;
	1) llvm-objdump -p "$@" | grep NEEDED | sed -e "s|^  NEEDED\( \)*\(.*\)|\2|g";;
	*) for f in $@; do llvm-objdump -p "$f" | grep 'NEEDED\|file format' | sed -e "s|^  NEEDED\( \)*\(.*\)|\2|g" -e "s|file format.*||g"; done
esac
